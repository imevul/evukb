/** Maximum corpora per multi-corpus ask or search request. */
export const maxMultiCorpora = 10;

/** Maximum file entries accepted in a portable `.evukb` zip import. */
export const maxPortableZipEntries = 10_000;

/** Maximum total uncompressed bytes accepted in a portable zip import. */
export const maxPortableZipUncompressedBytes = 500 * 1024 * 1024;
